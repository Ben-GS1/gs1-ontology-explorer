/**
 * Displayed in the footer (see App.tsx). Not tied to package.json's own
 * version field — this is a simple, manually-incremented build counter:
 * bump the last number by 1 for every change made to the app, starting
 * from v0.1.0.
 */
export const APP_VERSION = "0.1.0";
